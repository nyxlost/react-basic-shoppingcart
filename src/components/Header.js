import HeaderCart from "./HeaderCart"

const Header = () => {
    return(
        <header className="header">
            <h1>SHOP</h1>
            <HeaderCart/>
        </header>

    )
}

export default Header